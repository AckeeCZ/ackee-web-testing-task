import { connect as connectFela } from 'react-fela';
import ReferencesList from './ReferencesList';
import * as styles from './ReferencesList.styles';

export default connectFela(styles)(ReferencesList);
